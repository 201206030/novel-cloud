spring:
  application:
    name: news-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        ext‐config[0]:
          data‐id: novel-redis.yml
          group: novel-common
          refresh: true
